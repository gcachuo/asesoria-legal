import { AuthConfiguration } from "react-native-app-auth";

const config: AuthConfiguration = {
  clientId: "TU_CLIENT_ID",
  clientSecret: "TU_API_KEY",
  scopes: ["https://www.googleapis.com/auth/calendar"],
  issuer: "",
  redirectUrl: "",
};

export default config;
